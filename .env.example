# 设为 true 时使用本地 mock 数据（无后端也可开发）
VITE_USE_MOCK=true

# 直连后端时填写完整 origin；配合 Vite proxy 时可填 /api
VITE_API_BASE_URL=/api

# openapi-sync 脚本：Apifox / Swagger OpenAPI JSON 地址
APIFOX_OPENAPI_URL=https://your-team.apifox.com/xxx/openapi.json
